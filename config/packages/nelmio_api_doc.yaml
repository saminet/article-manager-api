nelmio_api_doc:
    documentation:
        info:
            title: Article Manager API
            description: API REST de gestion des articles
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
